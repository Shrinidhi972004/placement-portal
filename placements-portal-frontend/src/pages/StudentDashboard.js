function StudentDashboard() {
  return (
    <div>
      <h2>Welcome, {user.name} (Student)</h2>
      {/* TODO: List drives, apply, show applications */}
    </div>
  );
}

export default StudentDashboard;


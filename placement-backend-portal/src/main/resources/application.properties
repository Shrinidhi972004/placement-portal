# =========================
# = Database Configuration =
# =========================
spring.datasource.url=jdbc:postgresql://localhost:5432/placement_portal_db
spring.datasource.username=postgres
spring.datasource.password=Brunokumar@972004
spring.datasource.driver-class-name=org.postgresql.Driver

# ======================
# = JPA & Hibernate    =
# ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ================ Optional: Logging ================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ========== Spring Security (JWT placeholder) ======
# jwt.secret=your-very-secure-secret-key

# ================ (Optional) Server Port ===========
# server.port=8080

# ================ (Optional) CORS ==================
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=*

# =============== (Optional: Swagger/OpenAPI) =======
# springdoc.api-docs.enabled=true

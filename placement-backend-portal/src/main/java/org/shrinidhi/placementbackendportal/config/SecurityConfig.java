package org.shrinidhi.placementbackendportal.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // This class is now intentionally left empty to avoid bean conflicts with JwtSecurityConfig.
}
